# Server config (environment variables)
# Example: copy to .env and set values for your environment
PORT=4000
HOST=0.0.0.0
DATABASE_URL=postgresql://root:root@127.0.0.1:15432/craftless_db # CHANGED localhost to 127.0.0.1 otherwise "read ECONNRESET" error occurs

# ^^^^^^^^^ changed invoices_db to craftless_db
# CORS: leave empty or unset = allow all origins. Set to comma-separated origins to restrict.
# CORS_ORIGIN=http://localhost:5173,https://demo241.phitik.com
 
